apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.appName }}-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  airport-k8s-dashboard.json: |
    {{ .Files.Get "../../monitoring/dashboards/airport-k8s.json" | indent 4 }}